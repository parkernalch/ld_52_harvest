extends Node

signal cell_selected(cell)

signal tool_changed(action_tool)

signal water_level_changed(new_level)
signal torch_level_changed(new_level)
signal seed_level_changed(new_level)
signal ripe_count_changed(new_count)
signal spoiled_count_changed(new_count)

signal restart_game()
